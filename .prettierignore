node_modules
**/dist
raw
# Agent tool state, including the throwaway git worktrees Claude Code creates.
# Git does not list those — a worktree carries its own `.git` — so without this
# a background task running beside you fails `format:check` on files that are
# not the project's.
.claude
PLAN.md
PLAN.md.orig
package-lock.json

# Generated from the Zod in `server/src/system-schema.ts` by `npm run systems:schema`
# and compared against it byte for byte. Reformatting it breaks that comparison.
schema

# The test fixtures are a game system in the same shape as any other: their rules
# Markdown is a rulebook, whose table alignment is read rather than styled, and
# everything below is generated by `npm run fixture:build`. `system.json` is
# hand-written and stays formatted.
fixtures/*/rules
fixtures/*/tables
fixtures/*/items.json
fixtures/*/traits.json
